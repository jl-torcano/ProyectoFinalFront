import { UnaMayusculaDirective } from './una-mayuscula.directive';

describe('UnaMayusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new UnaMayusculaDirective();
    expect(directive).toBeTruthy();
  });
});
