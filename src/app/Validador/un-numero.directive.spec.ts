import { UnNumeroDirective } from './un-numero.directive';

describe('UnNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new UnNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
