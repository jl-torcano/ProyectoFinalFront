import { MinusculaDirective } from './minuscula.directive';

describe('MinusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new MinusculaDirective();
    expect(directive).toBeTruthy();
  });
});
