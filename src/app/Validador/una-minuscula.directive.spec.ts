import { UnaMinusculaDirective } from './una-minuscula.directive';

describe('UnaMinusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new UnaMinusculaDirective();
    expect(directive).toBeTruthy();
  });
});
