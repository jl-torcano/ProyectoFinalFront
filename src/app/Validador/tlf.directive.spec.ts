import { TlfDirective } from './tlf.directive';

describe('TlfDirective', () => {
  it('should create an instance', () => {
    const directive = new TlfDirective();
    expect(directive).toBeTruthy();
  });
});
