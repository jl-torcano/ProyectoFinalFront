import { CaracteresDirective } from './caracteres.directive';

describe('CaracteresDirective', () => {
  it('should create an instance', () => {
    const directive = new CaracteresDirective();
    expect(directive).toBeTruthy();
  });
});
